You are a senior HydroOJ developer. Read SECURITY.md and implement the security upgrade as described.

Specifically:
1. In addon/routes.ts, add helper functions requireAuth() and requirePerm() as described in SECURITY.md
2. Add empty POST handler stubs for: POST /rest-api/problems, POST /rest-api/contests, POST /rest-api/homework, POST /rest-api/trainings - each stub should call requireAuth() and requirePerm() with the correct permission, then return error NOT_IMPLEMENTED
3. All existing GET handlers should also call requireAuth() to ensure the user is logged in, but only verify identity not permissions for GET (keep existing behavior for read operations)
4. Use M().builtin.PERM.PERM_CREATE_PROBLEM, M().builtin.PERM.PERM_CREATE_CONTEST, M().builtin.PERM.PERM_CREATE_HOMEWORK, M().builtin.PERM.PERM_CREATE_TRAINING for permission constants
5. HTTP 403 for permission denied, HTTP 401 for not authenticated
6. Read hydro-restful-api/SPEC.md to understand existing endpoints first
7. After writing the code, run: git add -A && git commit -m '[Security] Add permission check framework and initial handlers' && git push -u origin security/permission-check
8. Use gh CLI to create a PR: gh pr create --title '[Security] Add permission check framework' --body 'Adds permission checks to all REST API handlers. Fixes missing authorization on write operations.'
9. Report the PR URL.
